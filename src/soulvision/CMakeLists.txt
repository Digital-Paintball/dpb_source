SET(SV_NAME "SoulVision")
SET(SV_MAJOR_VERSION 0)
SET(SV_MINOR_VERSION 1)
SET(SV_PATCH_VERSION 0)
SET(SV_VERSION "${SV_MAJOR_VERSION}.${SV_MINOR_VERSION}")
SET(SV_VERSION_FULL "${SV_VERSION}.${SV_PATCH_VERSION}")

SUBDIRS(sh sv)
